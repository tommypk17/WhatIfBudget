import { NeedWantFilterPipe } from './need-want-filter.pipe';

describe('NeedWantFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NeedWantFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
